<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.reimbursement.mapper.EmployeeMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.example.reimbursement.entity.Employee">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="name" property="name"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="department" property="department"/>
        <result column="department_id" property="departmentId"/>
        <result column="position" property="position"/>
        <result column="status" property="status"/>
        <result column="hire_date" property="hireDate"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 基本字段 -->
    <sql id="Base_Column_List">
        id, username, password, name, email, phone, department, department_id, position, status, hire_date, create_time, update_time
    </sql>

    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM employee
        WHERE id = #{id}
    </select>

    <!-- 根据用户名查询 -->
    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM employee
        WHERE username = #{username}
    </select>

    <!-- 查询所有 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM employee
        ORDER BY id DESC
    </select>

    <!-- 条件查询 -->
    <select id="selectByCondition" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM employee
        WHERE 1=1
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="department != null and department != ''">
            AND department = #{department}
        </if>
        ORDER BY id DESC
    </select>

    <!-- 获取用户角色编码 -->
    <select id="selectRoleCodesByUsername" resultType="string">
        SELECT r.code
        FROM role r
        INNER JOIN user_role ur ON r.id = ur.role_id
        INNER JOIN employee e ON ur.employee_id = e.id
        WHERE e.username = #{username}
    </select>

    <!-- 插入 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO employee (username, password, name, email, phone, department, department_id, position, status, hire_date)
        VALUES (#{username}, #{password}, #{name}, #{email}, #{phone}, #{department}, #{departmentId}, #{position}, #{status}, #{hireDate})
    </insert>

    <!-- 更新 -->
    <update id="update">
        UPDATE employee
        <set>
            <if test="password != null and password != ''">password = #{password},</if>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="department != null">department = #{department},</if>
            <if test="departmentId != null">department_id = #{departmentId},</if>
            <if test="position != null">position = #{position},</if>
            <if test="status != null">status = #{status},</if>
            <if test="hireDate != null">hire_date = #{hireDate},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 删除 -->
    <delete id="deleteById">
        DELETE FROM employee WHERE id = #{id}
    </delete>

    <!-- 根据部门名称查询员工ID列表 -->
    <select id="selectIdsByDepartment" resultType="java.lang.Integer">
        SELECT id
        FROM employee
        WHERE department = #{department}
    </select>

    <!-- 根据部门ID查询员工ID列表 -->
    <select id="selectIdsByDepartmentId" resultType="java.lang.Integer">
        SELECT id
        FROM employee
        WHERE department_id = #{departmentId}
    </select>

</mapper>
